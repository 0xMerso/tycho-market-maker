 Propositions de Renommage

  Section Global

  - pair_tag → trading_pair_tag
  - base_token → base_asset_symbol
  - base_token_address → base_asset_contract_address
  - quote_token → quote_asset_symbol
  - quote_token_address → quote_asset_contract_address
  - network_name → network_name
  - wallet_public_key → market_maker_wallet_address
  - gas_token_symbol → native_gas_token_address
  - gas_token_chainlink_price_feed → gas_price_oracle_address
  - tycho_api → tycho_api_endpoint

  Section Execution

  - min_watch_spread_bps → minimum_monitoring_spread_bps
  - min_executable_spread_bps → minimum_execution_spread_bps
  - max_slippage_pct → maximum_slippage_percentage
  - max_inventory_ratio → maximum_inventory_trade_ratio
  - tx_gas_limit → trade_gas_limit
  - block_offset → block_confirmation_offset
  - inclusion_block_delay → transaction_inclusion_delay_blocks
  - permit2_address → permit2_contract_address
  - tycho_router_address → tycho_router_contract_address

  Section Misc

  - poll_interval_ms → market_polling_interval_ms
  - publish_events → enable_event_publishing
  - skip_simulation → disable_transaction_simulation
  - infinite_approval → use_infinite_token_approval
  - min_publish_timeframe_ms → minimum_event_publish_interval_ms

  Section Price Feed

  - type → price_oracle_provider_type
  - source → price_feed_api_url
  - reverse → reverse_price_pair